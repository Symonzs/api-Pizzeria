meta {
  name: erreur format json (pas de base)
  type: http
  seq: 4
}

post {
  url: http://localhost:8080/pizzalandapi/pizzas/
  body: json
  auth: none
}

body:json {
  {
    "piname" : "pasdebase",
    "ingredients" : [
      1, 2, 3
    ],
    "pipate":"fine"
  }
}

docs {
  Fonctionne pas il faut au moins une !base, une pate et un ingredient
}
